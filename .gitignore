node_modules/
test-results/
playwright-report/

# Generated by `npm run build` / `npm run build:firefox` (scripts/build.mjs):
# the content script, popup, and options scripts are bundled from src/ and the
# manifest is copied from manifests/, so the extension is loadable only after a
# build.
extension/manifest.json
extension/content-script.js
extension/popup.js
extension/options.js
